# Render Production Configuration
# Copy these values to Render Environment Variables

# Data directories (adjust based on your deployment method)
OUTPUT_DIR=/data/outputs
DATA_DIR=/data/source

# CORS - IMPORTANT: Replace with your actual frontend URL(s)
CORS_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com

# Logging (INFO or WARNING recommended for production)
LOG_LEVEL=INFO

# Server configuration
PORT=8000
DEBUG=False

# Optional: If you add authentication later
# API_KEY=your-secret-api-key
# DATABASE_URL=your-database-connection-string
